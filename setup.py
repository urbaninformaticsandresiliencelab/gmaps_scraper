#!/usr/bin/env python

import setuptools

setuptools.setup(
    name = "gmaps_scraper",
    description = "Suite of tools for scraping place information using the "
                  "Google Maps API",
    packages = ["gmaps_scraper"]
)
